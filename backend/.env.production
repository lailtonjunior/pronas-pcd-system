# PRODUÇÃO - SISTEMA PRONAS/PCD
# ⚠️ ALTERAR TODOS OS VALORES ANTES DO DEPLOY

# Segurança - GERAR VALORES ÚNICOS
JWT_SECRET_KEY=${JWT_SECRET_KEY:-$(openssl rand -hex 64)}
ENCRYPTION_KEY=${ENCRYPTION_KEY:-$(openssl rand -hex 32)}

# Database - Configurar conforme ambiente
DATABASE_URL=postgresql://pronas_prod:${DB_PASS}@db.pronas.gov.br:5432/pronas_pcd

# APIs Governamentais
COMPRASNET_API_KEY=${COMPRASNET_KEY}
GOVBR_CLIENT_ID=${GOVBR_ID}
GOVBR_CLIENT_SECRET=${GOVBR_SECRET}

# Backup S3
AWS_ACCESS_KEY_ID=${AWS_KEY}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET}
BACKUP_S3_BUCKET=pronas-pcd-backups-prod

# Monitoramento
SENTRY_DSN=${SENTRY_DSN}
NEW_RELIC_LICENSE_KEY=${NEW_RELIC_KEY}